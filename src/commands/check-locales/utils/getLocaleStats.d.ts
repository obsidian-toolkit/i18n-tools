import { FlatObject } from '../../../types/definitions';
import { LocaleStat } from '../../../types/interfaces';
export declare function getLocaleStats(locale: string, enFlat: FlatObject): Promise<LocaleStat>;
